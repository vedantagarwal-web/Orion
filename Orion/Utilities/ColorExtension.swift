import SwiftUI

extension Color {
    static let primaryBrand = Color("PrimaryBrand")
    static let secondaryBrand = Color("SecondaryBrand")
    static let accentBrand = Color("AccentBrand")
    static let highlightBrand = Color("HighlightBrand")
}
